version: '3.8'

services:
  ai-image-edit:
    build: .
    ports:
      - "8000:8000"
    restart: unless-stopped
    volumes:
      - ai-image-edit-cache:/root/.cache/ai-image-edit

volumes:
  ai-image-edit-cache:
